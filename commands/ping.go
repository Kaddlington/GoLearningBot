package commands

/*
	func PingCommand() {
		discordgo.AddHandler(Ping)

		discordgo.Identify.Intents = discordgo.IntentsGuildMessages
	}

func Ping(s *discordgo.Session, m *discordgo.MessageCreate) {
	if m.Author.ID == s.State.User.ID {
		return
	}
	if m.Content == ">ping" {
		s.ChannelMessageSend(m.ChannelID, "Pong!")
	}
}

*/
